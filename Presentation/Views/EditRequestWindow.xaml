<Window x:Class="ClientWpf.Views.EditRequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление заявки" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Дата заявки:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker SelectedDate="{Binding RequestDate, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1"/>

        <TextBlock Text="Наименование:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding WorkName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1"/>

        <TextBlock Text="Описание:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Top"/>
        <TextBox Text="{Binding WorkDescription, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Height="60" AcceptsReturn="True" TextWrapping="Wrap"/>

        <TextBlock Text="Статус:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="3" Grid.Column="1"
                  ItemsSource="{Binding Statuses}"
                  SelectedItem="{Binding SelectedStatus, UpdateSourceTrigger=PropertyChanged}"
                  DisplayMemberPath="Name"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="80" Margin="5" IsDefault="True" Command="{Binding SaveCommand}"/>
            <Button Content="Отмена" Width="80" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
